import { SubscriptionItem } from './subscription-item/SubscriptionItem'

export function SubscriptionsMenu() {
	return (
		<nav>
			<ul>
				{/* {menu.map(menuItem => (
          <SubscriptionItem
            key={menuItem.link}
            item={menuItem}
          />
        ))} */}
			</ul>
		</nav>
	)
}
